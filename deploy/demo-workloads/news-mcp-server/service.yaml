apiVersion: v1
kind: Service
metadata:
  name: mcp-news-fetcher
  namespace: demo
  labels:
    app: news-fetcher
spec:
  selector:
    app: news-fetcher
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    protocol: TCP
  type: ClusterIP
